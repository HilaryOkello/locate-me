<div class="max-w-md mx-auto p-6">
  <div class="mb-6">
    <p class="text-lg text-gray-800">Hello <span class="font-medium"><%= @resource.email %></span>!</p>
  </div>

  <p class="mb-4 text-gray-700">Someone has requested a link to change your password. You can do this through the link below.</p>

  <div class="mb-6">
    <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token), 
          class: "inline-block bg-indigo-600 text-white py-2 px-4 rounded hover:bg-indigo-700" %>
  </div>

  <p class="text-sm text-gray-600 mb-2">If you didn't request this, please ignore this email.</p>
  <p class="text-sm text-gray-600">Your password won't change until you access the link above and create a new one.</p>
</div>