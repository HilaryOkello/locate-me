<div class="max-w-md mx-auto p-6">
  <div class="mb-6">
    <p class="text-lg text-gray-800">Hello <span class="font-medium"><%= @resource.email %></span>!</p>
  </div>

  <p class="mb-4 text-gray-700">Your account has been locked due to an excessive number of unsuccessful sign in attempts.</p>

  <p class="mb-4 text-gray-700">Click the link below to unlock your account:</p>

  <div class="mb-6">
    <%= link_to 'Unlock my account', unlock_url(@resource, unlock_token: @token), 
          class: "inline-block bg-indigo-600 text-white py-2 px-4 rounded hover:bg-indigo-700" %>
  </div>
</div>